GCS_BUCKET_NAME=
GCP_PROJECT_ID=
GCP_REGION=
GCP_ZONE=
BQ_DATASET=raw_settlens
DBT_DATASET=
# Optional: only needed if you're not already using Application Default
# Credentials (e.g. `gcloud auth application-default login`, or running on a
# GCE VM with an attached service account). Points auth at a key file instead.
GOOGLE_APPLICATION_CREDENTIALS=
AIRFLOW_UID=
AIRFLOW__API_AUTH__JWT_SECRET=
AIRFLOW_WWW_USER_PASSWORD=
